# 158 — Phase 12.9 Acceptance Closure

## Phase Record

```text
Phase: Phase 12 — Test Deployment & External Integration Validation
Subphase: 12.9 — Test observability, security, and recovery readiness
Status: Completed and accepted
Acceptance date: 2026-08-11
Validation plan: docs/156-phase-12.9-observability-security-recovery-readiness.md
HTTP hardening record: docs/157-phase-12.9-http-security-header-hardening.md
Validated application/security head: c6791cde5ae99a7b16d4582705f994b7963d115c
Previous accepted subphase: 12.8 — Full Internet E2E regression
Next subphase: 12.10 — Phase 12 validation and closure — In progress
Phase 13: Not started
```

## Closure Decision

Phase 12.9 is completed and accepted. The hosted Test deployment demonstrated sufficient observability, safe operational history, fail-closed security behavior, low-risk HTTP hardening, secret/client-exposure controls, incident-recovery readiness, and technical/dependency readiness to proceed to Phase 12.10.

This acceptance does not start Production work. Test remains `TRP_ENVIRONMENT=test`, uses the accepted developer-owned integrations, and keeps Vercel scheduler registration disabled.

## Accepted Stage Summary

### Stage A — Vercel Runtime and Build Log Hygiene

```text
Runtime Logs available: PASS
Secret/private iCal/auth/card/raw-provider sweep: PASS
Unexpected active hosted 5xx: none
Build-log secret hygiene: PASS
```

No secret value, private iCal URL/token, auth/session value, card data, database connection string, or raw provider payload was accepted as runtime/build evidence.

### Stage B — Durable Operational Observability

```text
Cron history: PASS
Normalized/safe cron result: PASS
Stable second-sync 0/0/0 evidence remains readable: PASS
Airbnb sync history and safe diagnostics: PASS
Private Airbnb URLs/tokens absent: PASS
No active duplicate regression: PASS
Email history/retry/attempt visibility: PASS
Email safe diagnostics/raw-provider absence: PASS
Financial/lifecycle reconstruction: PASS
Cancellation/refund separation: PASS
Exactly one intended refund identifiable: PASS
Sensitive/provider payload absence: PASS
```

The accepted durable histories remain the long-lived business diagnosis layer after Vercel Hobby Runtime Logs expire.

### Stage C — Hosted Auth and API Fail-Closed Surface

```text
Public route: PASS
/admin protection: PASS
Allowlisted Google login: PASS
No auth token in visible URL: PASS
Logout restores protected behavior: PASS
Auth cookie Secure/HttpOnly metadata: PASS
Cron without secret: HTTP 401 / safe CRON_UNAUTHORIZED / no execution created — PASS
/.env, /.env.local, /.git/config contents not exposed: PASS
Invalid Tilopay SDK-session payload: HTTP 400 / safe bounded error — PASS
```

No cookie value is retained in this closure record.

### Stage D — HTTP Security Baseline and Hardening

The initial hosted baseline had Vercel HSTS but lacked the application-owned low-risk headers. The accepted remediation commit is:

```text
155985a8263957cbe22c81df731ea1166e9101b1
fix(security): harden HTTP response headers
```

Final hosted posture:

```text
Strict-Transport-Security: max-age=63072000 — PASS
X-Content-Type-Options: nosniff — PASS
Referrer-Policy: strict-origin-when-cross-origin — PASS
Permissions-Policy: camera=(), microphone=(), geolocation=(), browsing-topics=() — PASS
X-Frame-Options: SAMEORIGIN — PASS
X-Powered-By: absent — PASS
```

Comprehensive CSP remains an explicit Phase 13 go-live gate. Tilopay loads an external SDK dynamically, so final Production CSP must be based on the complete measured script/connect/frame/image/style origin set and revalidated against Google/Auth.js, Cloudinary, admin UI, same-origin APIs, and Production Tilopay checkout. No broad wildcard CSP is accepted.

### Stage E — Static Secret and Client-Exposure Audit

```text
NEXT_PUBLIC audit: PASS
Console-output audit: PASS
Tracked secret-pattern audit: PASS
Real Airbnb token committed: no
Real PostgreSQL credential committed: no
Real Resend key committed: no
Real Tilopay credential committed: no
Browser CRON_SECRET exposure: no
Browser private Airbnb import URL exposure: no
Browser database credential exposure: no
Browser Resend/Cloudinary server credential exposure: no
Browser Tilopay server credential exposure: no
Card data sent to TRP backend: no
Repository status during audit: clean
```

Matches in `.env.example`, documentation, localhost test data, and `CHANGE_ME` placeholders were reviewed as intentional non-secret content.

### Stage F — Database Readiness and Approved Production Transfer

The owner decided on 2026-08-11 that Supabase backup/PITR/restore validation belongs to the separate company-owned Production database rather than the shared developer `portfolio-lab` Test project. This is an explicit scope transfer, not a hidden Test failure.

```text
F1 Test Supabase backup/PITR inspection: transferred to Phase 13 Production
F2 shared-project restore drill/boundary: transferred to Phase 13 Production
F3 Prisma schema validation: PASS
F3 migration status: PASS/current
Optional Test schema backup artifact: not required / not created
```

Phase 13 must define Production backup retention, RPO/RTO, PITR decision, restore procedure, and a safe restore rehearsal against isolated company-owned infrastructure.

### Stage G — Recovery and Incident-Response Tabletop

```text
General incident correlation/recovery: PASS
Payment/refund uncertain-result recovery: PASS
Email provider failure/retry recovery: PASS
Airbnb import failure/reconciliation recovery: PASS
Cron stale RUNNING recovery: PASS
Database disaster-recovery rehearsal: transferred to Phase 13 Production
```

The accepted financial rule remains explicit: never create a second payment/refund movement blindly when provider outcome is uncertain; reconcile provider evidence first and verify Payment, Refund, and Reservation state independently.

### Stage H — Dependency and Technical Readiness

The first runtime audit exposed 14 advisories, including critical Auth.js findings and high Next.js/transitive findings. The project did not run `npm audit fix --force`. Instead it applied a controlled security remediation:

```text
c6791cde5ae99a7b16d4582705f994b7963d115c
fix(security): update runtime dependencies and restrict Cloudinary images

next: 15.5.20 -> 15.5.23
next-auth: 5.0.0-beta.31 -> 5.0.0-beta.32
@auth/core resolved to 0.41.3
eslint-config-next: 15.5.20 -> 15.5.23
shadcn moved from runtime dependencies to devDependencies
Next remote image pattern restricted from all res.cloudinary.com paths to the configured TRP cloud/image/upload namespace
```

After remediation, `npm audit --omit=dev` retained four high-severity transitive advisories. They were individually reviewed rather than forcing a Next.js major upgrade:

```text
nanoid <3.3.17
- transitive below PostCSS
- TRP does not expose the vulnerable custom-generator size-zero path
- accepted as not applicable to TRP runtime

PostCSS findings
- transitive build/tooling paths
- TRP does not process attacker-controlled CSS at runtime
- accepted as not applicable to the deployed TRP execution path

sharp <0.35.0
- transitive under Next image optimization
- remote optimization input restricted to the configured TRP-owned Cloudinary namespace
- local and hosted Cloudinary image smoke passed after restriction
- accepted as mitigated for the current Test architecture
```

Final technical validation on the remediated state:

```text
env:validate: PASS
email:contract:validate: PASS
airbnb:import-policy:validate: PASS
airbnb:export-policy:validate: PASS
airbnb:export-path:validate: PASS
admin:calendar-display:validate: PASS
db:validate: PASS
db:migrate:status: PASS
lint: PASS
build: PASS
git diff --check: PASS
git status --short: CLEAN
hosted landing/auth/admin/logout smoke: PASS
hosted Cloudinary image smoke: PASS
hosted security headers: PASS
new unexpected hosted 5xx: none reported
```

12.9 therefore has no unresolved **applicable** high/critical dependency issue under the accepted Test architecture. Phase 13 must rerun the dependency audit against then-current supported versions before go-live rather than relying indefinitely on this dated applicability decision.

## Acceptance Accounting

The original 12.9 matrix listed 34 required criteria. The owner-approved Test boundary transferred three database-infrastructure criteria to Phase 13:

```text
F1 Production backup/PITR state
F2 Production restore boundary/rehearsal
G6 Production database disaster recovery
```

Final accounting:

```text
31 Test-applicable criteria: PASS
3 Production infrastructure criteria: explicitly transferred to Phase 13
0 unresolved applicable high/critical dependency findings
0 Test scheduler registrations introduced
0 Production resources/credentials introduced
```

## Explicit Phase 13 Carry-Forward

Phase 12.10 must preserve these Production gates for Phase 13:

```text
1. Provision the company-owned Production stack; do not reuse developer-owned Test provider accounts.
2. Replace static Test cron-disabled configuration with an environment-aware supported Vercel configuration that registers the four approved schedules only for TRP_ENVIRONMENT=production and fails closed for other/unknown environments.
3. Validate real Vercel scheduler recurrence and SCHEDULED execution history in Production.
4. Decide persistent runtime-log retention / Log Drain or another approved monitoring approach and Production alerting/escalation for cron, email, Airbnb, and uncertain financial reconciliation.
5. Implement and validate a comprehensive narrow CSP using measured Production provider/runtime origins.
6. Define Production Supabase backup retention, RPO/RTO, PITR requirements, restore procedure, and isolated restore rehearsal.
7. Rerun dependency/security audit on current supported versions before go-live and reassess any advisories previously classified as non-applicable/mitigated.
8. Migrate branding/media and all provider ownership to company-owned Production accounts before public launch.
```

## Non-Changes / Safety Boundary Preserved

12.9 did not:

```text
- activate any Vercel Test scheduler
- provision a Production resource or credential
- perform a destructive database restore
- create a new paid reservation/refund solely for observability testing
- intentionally break Airbnb, Resend, Google, Cloudinary, or Tilopay to manufacture failures
- store or document card data, cookies, auth tokens, provider secrets, or private iCal URLs
- introduce a monitoring vendor solely to satisfy Test acceptance
- jump to Next.js 16 or use npm audit fix --force
- change reservation, cancellation, refund, date-change, stay-extension, email, or iCal business rules
```

## Final Status

```text
12.9 — Completed and accepted on 2026-08-11
Validated application/security head: c6791cde5ae99a7b16d4582705f994b7963d115c
12.10 — Phase 12 validation and closure — In progress
Phase 13 — Not started
```

12.10 must now reconcile all Phase 12 subphase records, verify the Production carry-forwards above, and formally close Phase 12 before any Production provisioning or go-live work begins.
