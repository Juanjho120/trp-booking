# 153 — Phase 12.8 Full Internet E2E Regression Start

## Phase Record

```text
Phase: Phase 12 — Test Deployment & External Integration Validation
Subphase: 12.8 — Full Internet E2E regression
Status: In progress
Start date: 2026-08-11
Start/base head: a2b1f344a16010c5afff045a8114e11bc2ab91b6
Previous decision: docs/152-phase-12.7-vercel-cron-deployment-and-scheduler-validation.md
Next planned subphase after acceptance: 12.9 — Test observability, security, and recovery readiness
Production work: excluded; Phase 13 only
```

## Goal

Run the hosted Internet-accessible Test regression against the already-accepted TRP Booking deployment and real Test integrations without introducing Production infrastructure.

12.8 begins after the explicit scheduler decision recorded in 12.7:

```text
Test remains on Vercel Hobby
Vercel scheduler registration remains disabled
recurring scheduler acceptance moves to Phase 13
```

## Starting State to Preserve

```text
TRP_ENVIRONMENT=test
https://trp-booking.juantzun.dev operational
shared Local/Test Supabase accepted
hosted Google OAuth accepted
Tilopay sandbox accepted
Test Resend/Zoho routing accepted
Cloudinary Test integration accepted
real Airbnb inbound and outbound iCal connections accepted
12.6 anti-feedback/recovery/idempotency accepted
vercel.json crons = []
```

Do not rotate provider credentials, Airbnb tokens, export tokens, or CRON_SECRET merely to begin 12.8.

## Scheduler Boundary During 12.8

Automatic Vercel recurrence is not part of the Test E2E matrix.

When an E2E case needs to exercise a cron-owned business service, use the existing protected admin cron console or another already-accepted manual path and validate the resulting business state/history.

Do not require:

```text
- Test Vercel Pro
- Vercel Cron registration
- recurring SCHEDULED history
- cron timing precision
```

Those are Phase 13 production-deployment concerns.

## First 12.8 Task

Before running destructive or provider-affecting scenarios, derive the exact reduced Internet E2E matrix from the current repository and previously accepted Phase 8–12 contracts.

The matrix must identify:

```text
- which hosted public flows need revalidation
- which admin flows need revalidation
- which external-provider paths need real Test evidence
- which already-accepted destructive/financial scenarios can be reduced to targeted regression checks
- which cron-owned services are exercised manually
- which data can be safely created and cleaned up in the shared Local/Test database
- which evidence belongs to later 12.9 observability/security/recovery work instead
```

Do not repeat every historical matrix merely because it exists. Prefer a focused cross-phase regression that proves the hosted application still behaves coherently after the Phase 12 integration work.

## Phase Boundary

12.8 must not:

```text
- activate Vercel Cron in Test
- upgrade Test to Vercel Pro solely for schedulers
- create Production provider accounts
- move Production DNS
- use Production Tilopay credentials
- create Production Resend/Zoho/Cloudinary/Supabase resources
- change the approved cancellation/refund/date-change business policies
- expand TRP Booking into PMS behavior
```

Production infrastructure and scheduler activation remain Phase 13 work.
