# 151 — Phase 12.6 Acceptance Closure

## Phase Record

```text
Phase: Phase 12 — Test Deployment & External Integration Validation
Subphase: 12.6 — TRP Booking Test outbound iCal and controlled Airbnb round-trip
Status: Completed and accepted
Acceptance date: 2026-08-10
Accepted head: 543e0b4bc4cd700e6ebc3a29415981aeae91a13c
12.6.1 implementation commit: 5a120e49fb2e6196d64fc98e608552b217b7522f
12.6.1.1 implementation/accepted head: 543e0b4bc4cd700e6ebc3a29415981aeae91a13c
Previous accepted subphase: 12.5
Next subphase: 12.7 — Vercel Cron deployment and scheduler validation
Production work: excluded; Phase 13 only
```

## Accepted Outcome

Phase 12.6 proved the real bidirectional availability contract without allowing provider-origin state to become a new TRP source of truth.

```text
TRP-owned unavailability
-> private TRP Test .ics feed
-> matching real Airbnb listing
-> Airbnb availability
-> Airbnb export feed
-> TRP inbound sync
-> stable effective availability
```

The reverse/recovery path also passed:

```text
TRP source removed
-> TRP outbound range removed
-> Airbnb imported block removed after refresh
-> TRP reflected provider event reconciled/removed
-> related AIRBNB blocks soft-deleted
-> availability restored
```

## 12.6.1 — Outbound Ownership and Stable Identity

Accepted behavior:

```text
Include from TRP ownership:
- CONFIRMED direct reservations
- effective direct-reservation preparation buffers
- MANUAL_BLOCK
- MAINTENANCE
- non-provider PREPARATION_BUFFER records needed for direct override/suppression semantics

Exclude provider/reflection state:
- AIRBNB
- PREPARATION_BUFFER linked to externalCalendarEventId
- provider-import artifacts
- physical COMPOSED_LISTING_DEPENDENCY copies
- DIRECT_RESERVATION CalendarBlock copies as a duplicate source of truth
```

The dynamic composed-listing matrix remained authoritative:

```text
Apartment source -> Apartment + Complete
Bungalow source  -> Bungalow + Complete
Complete source  -> Complete + Apartment + Bungalow
```

VEVENT identity was frozen before the real provider connection:

```text
trp-booking-<externalCalendarId>-<startDate>-<endDate>@turefugioperfecto.com
```

Unrelated range insertion/removal did not rename unchanged semantic ranges.

## 12.6.1.1 — Airbnb `.ics` Compatibility

Airbnb-facing private URLs use:

```text
/api/ical/<RAW_PRIVATE_TOKEN>.ics
```

The route strips only the `.ics` suffix before hashing, so the already-generated raw token and persisted SHA-256 hash remain unchanged. The extensionless path remains diagnostic compatibility.

All hosted checks passed:

```text
3 x HTTP 200
3 x Content-Type text/calendar
.ics and extensionless forms resolve to equivalent semantic feed content
existing exportTokenHash values unchanged
VEVENT UIDs stable
raw tokens absent from Vercel Runtime Logs
```

## ExternalCalendar Operational State

The same three records created during 12.5 were retained. No duplicate ExternalCalendar rows were created.

Accepted state:

```text
provider: AIRBNB
direction: BIDIRECTIONAL
isImportEnabled: true
isExportEnabled: true
status: ACTIVE
exportTokenHash: populated SHA-256 only
lastExportGeneratedAt: populated after feed access
```

Three independent cryptographically strong raw export tokens were generated privately by the operator and were never committed, pasted into ChatGPT, stored in plaintext database columns, or intentionally logged.

## Pre-Provider Outbound Matrix

Three isolated one-day `MANUAL_BLOCK` signals passed before connecting Airbnb:

```text
Apartment source:
- Apartment feed: present
- Complete feed: present
- Bungalow feed: absent

Bungalow source:
- Bungalow feed: present
- Complete feed: present
- Apartment feed: absent

Complete source:
- Complete feed: present
- Apartment feed: present
- Bungalow feed: present
```

After release, all three temporary signals disappeared from every feed where they had appeared. Persisted history remained soft-deleted/auditable.

## Real Airbnb Connection

The three feeds were connected one-by-one using the strict mapping:

```text
TRP Apartment -> Airbnb Apartamento Blanco y Negro
TRP Bungalow  -> Airbnb Bungalow Refugio Perfecto
TRP Complete  -> Airbnb Refugio Completo
```

Provider-side calendar name:

```text
TRP Booking Test
```

Airbnb successfully consumed TRP-owned manual blocks from all three feeds. No unexpected dates appeared solely because provider-origin state was echoed back.

## Controlled Round-Trip

An isolated Apartment manual block was used as the strongest sibling-contamination check.

Expected and observed effective availability:

```text
Apartment: blocked
Complete: blocked through dependency
Bungalow: available
```

After Airbnb imported the TRP signal and the operator manually ran `SYNC_AIRBNB_CALENDARS`, the result remained stable:

```text
no range expansion
no feedback-generated preparation days
no Bungalow contamination through a reflected Complete blocker
TRP outbound still excluded provider-origin reflection state
```

## Recovery and Idempotency

The controlled TRP manual block was released through the protected admin flow.

Accepted recovery:

```text
TRP outbound no longer contained the signal
Airbnb removed the external-calendar unavailability after refresh
TRP inbound reconciliation removed/reflected provider history safely
related active AIRBNB blocks were soft-deleted
Apartment availability recovered
Bungalow availability remained correct
Complete availability recovered
```

A second unchanged inbound sync was then executed. It was idempotent, and the duplicate check for active `AIRBNB`/`PREPARATION_BUFFER` ranges returned:

```text
0 rows
```

## Security and Audit Acceptance

Accepted:

```text
- no raw Airbnb import token in committed files
- no raw TRP export token in committed files
- no raw import/export token observed in Vercel Runtime Logs
- no private feed URL stored in docs/tickets/chat
- feed content contains only generic unavailable event data
- independent provider/TRP persistence remains auditable
- no hard delete introduced
```

## Scheduler Boundary Preserved

Throughout 12.6:

```json
{
  "crons": []
}
```

The four application cron handlers and the protected `/admin/cron-jobs` manual console remained available, but Vercel scheduler registration stayed intentionally disabled.

Scheduler deployment belongs exclusively to 12.7.

## Acceptance Matrix

```text
[x] 12.6.1 provider-loop-prevention correction accepted
[x] provider-origin Airbnb state excluded from outbound feeds
[x] direct/manual/maintenance ownership contract protected
[x] composed-listing outbound matrix accepted
[x] stable VEVENT identity accepted
[x] permanent UID namespace accepted
[x] three existing calendars safely made bidirectional/export-capable
[x] unique raw tokens generated privately
[x] only hashes persisted
[x] three safe `.ics` feeds validated
[x] raw export tokens absent from Runtime Logs
[x] three feeds connected to matching real Airbnb listings
[x] controlled TRP-owned signal reflected in Airbnb correctly
[x] Airbnb -> TRP return did not expand/cross-contaminate availability
[x] source removal propagated and availability recovered
[x] repeated stable sync idempotent
[x] active duplicate block query returned 0 rows
[x] vercel.json remained crons = []
```

## Handoff

Phase 12.6 is closed.

```text
12.6 — Completed and accepted
12.6.1 — Completed and accepted
12.6.1.1 — Completed and accepted
12.7 — Vercel Cron deployment and scheduler validation — In progress
```

Continue with `docs/152-phase-12.7-vercel-cron-deployment-and-scheduler-validation.md`.
