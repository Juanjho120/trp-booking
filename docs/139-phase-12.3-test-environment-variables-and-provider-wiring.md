# 139 — Phase 12.3 Test Environment Variables and Provider Wiring

## Subphase Record

```text
Phase: 12 — Test Deployment & External Integration Validation
Subphase: 12.3 — Test environment variables and provider wiring
Status: In progress — Vercel configuration, redeploy, and acceptance checks pending
Accepted Vercel baseline: 3bf86cc946392649b1284f7e8dc67fd0c18749f5
Date: 2026-08-10
Production work: excluded; Phase 13 only
```

## Objective

12.3 completes the environment-variable/provider configuration for the already-created Test
Vercel project. It does not create a second Test infrastructure stack and it does not begin
Production provisioning.

The approved deployment model remains:

```text
Vercel project: dedicated TRP Booking Test project in the developer-owned account
Vercel environment: Production
Production branch: main
TRP business/runtime environment: TRP_ENVIRONMENT=test
Current hosted URL: Vercel-generated HTTPS *.vercel.app baseline
Stable Test target: https://trp-booking.juantzun.dev
Custom-domain acceptance: 12.4
```

## Provider Ownership Contract

Test intentionally reuses the same developer-owned provider accounts already used by Local:

```text
Supabase: same database as Local
Resend: same personal account and verified sending domain as Local
Zoho: same juantzun.dev organization and aliases as Local
Cloudinary: same personal account as Local
Tilopay: same sandbox account as Local
Google/Auth.js credentials: same current developer Test credentials
```

Test-specific configuration remains:

```text
TRP_ENVIRONMENT=test
CRON_SECRET=<dedicated Test-only secret>
EMAIL_TEST_RECIPIENT absent/empty
```

No Production-company account, credential, domain, database, or mailbox is introduced in
12.3.

## Step 1 — Audit the Existing 12.2 Core Wiring

In Vercel, open:

```text
TRP Booking Test project
→ Settings
→ Environment Variables
→ filter/select Production
```

Do not rotate values that are already valid. Confirm that the following variables exist in
the Vercel **Production** environment because the Test project uses a Vercel Production
Deployment while TRP itself runs as `test`:

```text
DATABASE_URL
DIRECT_URL

AUTH_SECRET
AUTH_GOOGLE_ID
AUTH_GOOGLE_SECRET
AUTH_ALLOWED_ADMIN_EMAILS
AUTH_TRUST_HOST=true

CLOUDINARY_CLOUD_NAME
CLOUDINARY_API_KEY
CLOUDINARY_API_SECRET
CLOUDINARY_UPLOAD_FOLDER

TILOPAY_API_KEY
TILOPAY_API_USER
TILOPAY_API_PASSWORD
TILOPAY_ENVIRONMENT=sandbox
TILOPAY_REDIRECT_URL=https://trp-booking.juantzun.dev/api/payments/tilopay/redirect
TILOPAY_SUCCESS_URL=https://trp-booking.juantzun.dev/reservas/pago/exitoso
TILOPAY_CANCEL_URL=https://trp-booking.juantzun.dev/reservas/pago/cancelado
TILOPAY_ERROR_URL=https://trp-booking.juantzun.dev/reservas/pago/error
TILOPAY_WEBHOOK_URL=https://trp-booking.juantzun.dev/api/payments/tilopay/webhook

TRP_ENVIRONMENT=test
CRON_SECRET=<existing dedicated Test secret>
```

The Tilopay URLs remain **configuration targets only** in 12.3. Their external callback
behavior is accepted only after the stable Test domain is attached in 12.4.

## Step 2 — Add the Complete Test Resend/Email Contract

Reuse the same `RESEND_API_KEY` already used by Local. Do not create a new Test Resend
account.

Add/update these Vercel Production-environment variables:

```text
RESEND_API_KEY=<same developer-owned Resend API key used by Local>

EMAIL_DELIVERY_MODE=test
EMAIL_FROM_ES=Tu Refugio Perfecto Test <reservas@mail.trp-booking.juantzun.dev>
EMAIL_FROM_EN=Tu Refugio Perfecto Test <reservations@mail.trp-booking.juantzun.dev>
EMAIL_REPLY_TO_ES=reservas@juantzun.dev
EMAIL_REPLY_TO_EN=reservations@juantzun.dev
EMAIL_ADMIN_RECIPIENTS=admin@juantzun.dev
EMAIL_PUBLIC_BASE_URL=https://trp-booking.juantzun.dev
EMAIL_BRAND_LOGO_URL=https://res.cloudinary.com/juan-tzun-portfolio/image/upload/v1784668172/trp-booking/brand/logo-primary.png
```

`EMAIL_TEST_RECIPIENT` must **not** contain the Local personal mailbox in Test. Keep it
undefined or empty. Test guest delivery uses the intended reservation recipient and Test
admin delivery uses `admin@juantzun.dev`.

The Reply-To aliases preserve the accepted Package F boundary:

```text
Spanish human replies -> reservas@juantzun.dev -> Zoho
English human replies -> reservations@juantzun.dev -> Zoho
```

No additional Zoho, SPF, DKIM, DMARC, MX, Resend-domain, or Cloudinary setup is required in
12.3. Those Local/Test provider resources are intentionally reused.

## Step 3 — Keep Later-Phase Variables and Integrations Deferred

Do not add/activate these yet:

```text
AUTH_URL
AIRBNB_ICAL_IMPORT_URLS_JSON
```

`AUTH_URL` and the hosted Google OAuth/callback boundary belong to 12.4.
`AIRBNB_ICAL_IMPORT_URLS_JSON` belongs to 12.5.

Do not restore the Vercel schedules. `vercel.json` must remain:

```json
{
  "crons": []
}
```

The approved 5/30-minute schedules return unchanged in 12.7 after the Test project operates
on Vercel Pro.

## Step 4 — Redeploy after Environment Changes

Vercel environment-variable changes do not modify an already-created deployment. After the
12.3 variables are saved, create a **new Production Deployment** from the current `main`
baseline.

Dashboard flow:

```text
Project
→ Deployments
→ latest accepted Production Deployment
→ ...
→ Redeploy
→ Redeploy to Production
```

A new commit also creates a Production Deployment, but 12.3 does not require an application
code change merely to apply provider variables.

Do not change the Build Command, Install Command, Output Directory, framework preset, or
Node version to make environment validation pass. If the new deployment fails, use the first
meaningful build/runtime configuration error to correct the environment contract.

## 12.3 Hosted Acceptance Matrix

12.3 remains open until all checks below pass after the new deployment:

| # | Check | Expected result |
|---|---|---|
| 1 | Deployment | New Vercel Production Deployment reaches `Ready` |
| 2 | TRP environment | `TRP_ENVIRONMENT=test` contract remains valid |
| 3 | Database | Public property/availability reads continue working against the shared Local/Test Supabase database |
| 4 | Cloudinary | Existing public property/brand images load successfully |
| 5 | Tilopay configuration | Sandbox credentials and approved Test target URLs pass application configuration validation; no callback E2E is claimed |
| 6 | Resend mode | `EMAIL_DELIVERY_MODE=test` is accepted by application configuration |
| 7 | ES sender | `reservas@mail.trp-booking.juantzun.dev` contract is configured |
| 8 | EN sender | `reservations@mail.trp-booking.juantzun.dev` contract is configured |
| 9 | ES Reply-To | `reservas@juantzun.dev` is configured |
| 10 | EN Reply-To | `reservations@juantzun.dev` is configured |
| 11 | Admin recipient | `admin@juantzun.dev` is configured |
| 12 | Guest routing isolation | `EMAIL_TEST_RECIPIENT` is absent/empty in Test |
| 13 | Brand asset | Permanent developer-owned Cloudinary logo URL is configured |
| 14 | Public base target | `https://trp-booking.juantzun.dev` is configured, while actual link/domain acceptance remains deferred to 12.4 |
| 15 | Auth boundary | `AUTH_URL` remains deferred; hosted Google login is not claimed as accepted |
| 16 | Airbnb boundary | `AIRBNB_ICAL_IMPORT_URLS_JSON` remains deferred to 12.5 |
| 17 | Scheduler boundary | `vercel.json` remains `crons: []`; no Vercel cron execution occurs |
| 18 | Security | No secret values are committed to Git or exposed in public/client-facing output |
| 19 | Baseline smoke | ES/EN landing and at least one property page continue loading without fatal runtime errors |

A real transactional email send is **not required to close 12.3**. The objective here is the
hosted provider/environment contract. Full Internet E2E—including real reservation email
delivery—is retained for 12.8, after the stable Test domain and external callbacks have been
validated.

## Local Technical Gate

Before recording acceptance, the repository should still pass its existing technical gate:

```text
npm run email:contract:validate
npm run env:validate
npm run db:validate
npm run lint
npm run build
git diff --check
```

Do not paste secrets into documentation, tickets, chat messages, screenshots, or commits.

## Acceptance Outcome

Current state:

```text
12.3: IN PROGRESS
Vercel Production environment audit: pending
Complete Resend/email Test wiring: pending
Environment-variable redeploy: pending
Hosted acceptance matrix: pending
12.4 activation: blocked until 12.3 acceptance
```

When all checks pass, create the 12.3 acceptance closure and advance to:

```text
12.4 — Test custom domain, Auth.js, and external callback validation
```
