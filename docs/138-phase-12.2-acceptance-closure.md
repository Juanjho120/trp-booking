# 138 — Phase 12.2 Acceptance Closure

## Closure Record

```text
Phase: 12 — Test Deployment & External Integration Validation
Subphase: 12.2 — Vercel Test project and first deployment
Status: Completed and accepted
Acceptance date: 2026-08-10
Accepted deployment source head: 91f513c57b6220ad8d1d32f9a198a3d5099b1fd7
Implementation/correction record: docs/137-phase-12.2-vercel-test-project-and-first-deployment.md
Production work: excluded; Phase 13 only
Next subphase: 12.3 — Test environment variables and provider wiring
```

## Accepted Outcome

12.2 established TRP Booking's first real hosted Test baseline in the developer-owned Vercel account.

The accepted deployment boundary is:

```text
Git repository: Juanjho120/trp-booking
Production branch: main
Vercel project: dedicated TRP Booking Test project
Vercel deployment environment: Production
TRP business/runtime environment: test
Hosted baseline: Vercel-generated HTTPS *.vercel.app deployment
Stable Test domain: trp-booking.juantzun.dev — not attached yet
Email delivery: disabled for the 12.2 baseline
Vercel cron schedules: intentionally not registered
Production/company infrastructure: untouched
```

The exact temporary Vercel-generated alias is not persisted as a long-lived repository contract. Vercel remains the source of truth for that deployment metadata, while `trp-booking.juantzun.dev` remains the stable Test-domain contract that will be attached and validated in 12.4.

## Blocker and Correction Accepted

The initial project/deployment attempt was rejected before any deployment record was created because the Vercel Test project was on Hobby while the repository declared cron schedules every 5 and 30 minutes.

The accepted correction is:

```json
{
  "crons": []
}
```

This correction only defers Vercel scheduler registration. It does not delete or change the four cron API handlers, their authorization, `CRON_SECRET`, or their approved frequencies.

The approved schedules remain frozen for 12.7:

```text
/api/cron/sync-airbnb-calendars -> */30 * * * *
/api/cron/expire-pending-reservation-holds -> */5 * * * *
/api/cron/process-email-notifications -> */5 * * * *
/api/cron/schedule-arrival-instructions -> */30 * * * *
```

12.7 must restore these schedules exactly when the Test project is operated on a Vercel plan that supports them. They must not be weakened to once-per-day schedules merely to fit Hobby.

## Acceptance Evidence

On 2026-08-10, after the cron-registration correction was committed, the owner reported that the first deployment completed successfully and worked without problems.

Accepted evidence:

```text
- a real Vercel deployment record now exists;
- the first hosted deployment completed successfully;
- the accepted deployment source is 91f513c57b6220ad8d1d32f9a198a3d5099b1fd7;
- the generated HTTPS baseline is operational;
- no cron schedules are registered from vercel.json;
- no stable custom domain was accepted as part of 12.2;
- no Phase 13 production/company infrastructure was introduced.
```

This acceptance closes the hosted-baseline objective. It does not claim acceptance of the integrations intentionally deferred to later Phase 12 subphases.

## Deferred Boundaries

The following remain outside 12.2:

```text
12.3 — complete Test environment variables and provider wiring
12.4 — attach trp-booking.juantzun.dev; validate Auth.js and external callbacks
12.5 — validate real Airbnb inbound iCal
12.6 — expose/validate TRP Test outbound iCal against Airbnb
12.7 — restore Vercel cron schedules and validate the real scheduler
12.8 — full Internet E2E regression
12.9 — observability, security, and recovery readiness
12.10 — Phase 12 validation and closure
```

In particular, successful 12.2 deployment does not yet constitute acceptance of hosted Google admin login, Tilopay callback behavior, transactional email delivery, Airbnb synchronization, custom-domain routing, or Vercel Cron execution.

## Final State

```text
12.1 — Completed and accepted
12.2 — Completed and accepted
12.3 — Not started; next
Phase 12 — In progress
Phase 13 — Not started
vercel.json crons — intentionally empty through 12.6
```
